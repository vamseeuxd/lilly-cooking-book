import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lilly-cooking-book',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
